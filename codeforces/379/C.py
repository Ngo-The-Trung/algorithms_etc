#!/usr/bin/env python
# TODO Incomplete
import sys

lines = sys.stdin.read().splitlines()
n, m, k = [int(v) for v in lines[0].split(" ")]
x, s = [int(v) for v in lines[1].split(" ")]
a = [int(v) for v in lines[2].split(" ")]
b = [int(v) for v in lines[3].split(" ")]
